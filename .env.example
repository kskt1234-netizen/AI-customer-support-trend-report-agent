# 이 파일을 복사해 '.env'로 만들고 실제 값을 채우세요. (.env는 git에 올리지 않습니다)

# OpenAI API 키 — real 모드 테스트 및 실제 실행에 필요.
OPENAI_API_KEY=sk-여기에-실제-키-붙여넣기

# 사용할 LLM 공급사. 비워두면 factory가 'openai'를 기본으로 씁니다.
LLM_PROVIDER=openai

# 테스트 모드. mock(기본, 무료/빠름) 또는 real(실제 API 호출/정확도 측정).
TEST_MODE=mock
